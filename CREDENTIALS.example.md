# PNBrad credentials — EXAMPLE ONLY

Copy to `CREDENTIALS.md` locally (gitignored). Never commit real secrets.

| Variable | Notes |
|----------|-------|
| DB_PASSWORD | Strong random |
| ROOT_PASSWORD | Container SSH |
| RADIUS_SECRET | FreeRADIUS shared secret |

See `docker-compose.yml` environment block.
